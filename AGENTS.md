# AGENTS.md

## Project Name

bp_tracker_app

## use venv path

このプロジェクトでは、Python の仮想環境を使用してください。

```bash
D:\Dev\venvs\venv_bptrack312\Scripts\python.exe
```

## Purpose

このプロジェクトは、血圧・脈拍・服薬・飲酒・立ちくらみ・体調メモなどを記録し、血圧変動のトリガーを探すための Python GUI アプリです。

このアプリは医療診断を行うものではありません。記録を整理し、医師に相談するための補助ツールです。

## Language

コードコメント、README、画面表示、エラーメッセージは、基本的に日本語で記述してください。

## Target User

Python初心者でも理解しやすい構成を優先してください。

ユーザーは Windows 11 環境で VS Code を使用します。将来的に PyInstaller で exe 化する可能性があります。

## Development Policy

- いきなり大きな構成にしないでください。
- まずは動作する最小構成を優先してください。
- 医療判断を断定せず、「注意」「確認推奨」などの表現に留めてください。
- データは SQLite に保存してください。
- GUI は tkinter を使用してください。
- グラフは matplotlib を使用してください。
- 表示・集計には pandas を使用しても構いません。
- 既存機能の安定動作を優先し、機能追加は段階的に行ってください。

## Project Structure

```text
bp_tracker_app/
├─ main.py
├─ db.py
├─ analysis.py
├─ graph_view.py
├─ export_csv.py
├─ constants.py
├─ data/
│  └─ bp_tracker.sqlite3
├─ exports/
├─ reports/
├─ backups/
└─ README.md
```

## Database

SQLite を使用してください。DB ファイルの保存先は以下です。

```text
data/bp_tracker.sqlite3
```

`data/` フォルダが存在しない場合は、自動作成してください。既存DBに列を追加する場合は、保存済みデータを消さず、不足列だけを追加してください。

## Main Table

1テーブル構成を基本とし、`bp_records` に日別記録を保存します。`record_date` は `TEXT NOT NULL UNIQUE` としてください。

主な記録項目:

- 記録日
- 起床直後、座位、排尿後座位、立位1分後、立位3分後の血圧と脈拍
- 起床時刻、就寝時刻、睡眠時間、体重
- コンサータ服用時刻
- ジャディアンス服用有無
- メトグルコ服用回数
- 立ちくらみ
- 前夜飲酒
- 水分量
- 背部痛、耳痛・顔面痛
- 食事、塩分、カフェイン、運動、外出、入浴、睡眠の質、ストレス度、PC作業時間
- 首・肩・顎の張り、食いしばり、頭痛、背部痛詳細、めまい・ふらつき詳細
- 自由メモ

体重や生活記録の追加項目は任意入力です。未入力の場合は `NULL` として保存してください。

## Dropdown Values

以下の項目は Combobox を使ってください。

- jardiance: `有`, `無`, `未入力`
- dizziness: `無`, `軽い`, `強い`, `未入力`
- drinking_prev_night: `無`, `少量`, `多い`, `未入力`
- water_amount: `少ない`, `普通`, `多い`, `未入力`
- back_pain / ear_face_pain / outing / bathing / headache など: `無`, `有`, `未入力`
- caffeine: `無`, `少量`, `多い`, `未入力`
- sleep_quality: `良い`, `普通`, `悪い`, `未入力`
- stress_level: `低い`, `普通`, `高い`, `未入力`

## GUI Requirements

tkinter を使用してください。

メイン画面には以下を配置してください。

- 入力フォーム
- 入力補助
- 保存ボタン
- 選択行読み込みボタン
- 更新ボタン
- 削除ボタン
- 全期間表示ボタン
- 期間指定表示ボタン
- CSV出力ボタン
- グラフ表示ボタン
- Hi-Level Sample 抽出ボタン
- Low-Level Sample 抽出ボタン
- マークポイント表示ボタン
- トリガー分析表示ボタン
- 診察用レポート出力ボタン
- バックアップ作成ボタン
- DB整合性チェックボタン
- Treeview による一覧表示

## Analysis Requirements

医学的診断はしないでください。表示は以下のような表現にしてください。

- 注意候補
- 確認推奨
- 通常範囲
- データ不足
- 追加記録推奨

### Mark Points

- 起床直後と排尿後座位の差: `wpH - auH`
- 起床直後と座位の差: `wpH - spH`
- 座位と立位1分後の差: `spH - st1H`
- 座位と立位3分後の差: `spH - st3H`

座位から立位で、収縮期血圧が 20mmHg 以上低下、または拡張期血圧が 10mmHg 以上低下した場合は、以下のように表示してください。

```text
起立性低血圧の可能性あり。医師に相談するための記録として保存してください。
```

ただし、診断とは書かないでください。

### High Level Sample

- 収縮期血圧が 140 以上
- または拡張期血圧が 90 以上
- またはメモに「背部痛」「胸痛」「息苦しい」などが含まれる

表示名は `Hi-Level Sample` としてください。

### Low Level Sample

- 収縮期血圧が 100 未満
- または座位から立位1分後で収縮期血圧が 20 以上低下
- または座位から立位3分後で収縮期血圧が 20 以上低下
- または立ちくらみが「強い」

表示名は `Low-Level Sample` としてください。

### Trigger Analysis

以下の条件別集計を追加してください。

- 前夜飲酒あり / なし の朝血圧平均比較
- ジャディアンス服用あり / なし の血圧比較
- コンサータ服用時刻別の血圧比較
- 水分量別の血圧比較
- 睡眠時間別の血圧比較
- 立ちくらみあり / なし の血圧比較
- 背部痛あり / なし の血圧比較

## Graph Requirements

matplotlib を使用してください。グラフタイトル、軸ラベルは日本語で表示してください。

基本グラフ:

- 朝の収縮期血圧グラフ
- 朝の拡張期血圧グラフ
- 脈拍グラフ
- 血圧差グラフ

拡張グラフは見やすさを優先し、1つの画面に詰め込みすぎないでください。

## Export Requirements

CSV 出力機能を作成してください。保存先は `exports/` フォルダです。

ファイル名例:

```text
bp_records_20260921.csv
bp_records_20260901_20260921.csv
```

## Backup Requirements

SQLite DB のバックアップ機能を追加してください。

- `backups/` フォルダを自動作成
- ボタンで手動バックアップ
- バックアップファイル名に日時を入れる
- バックアップは上書きしない
- 削除機能は最初から付けなくてよい

ファイル名例:

```text
bp_tracker_backup_20260923_224500.sqlite3
```

## Data Check Requirements

以下の整合性チェックを追加してください。

- DB接続確認
- テーブル存在確認
- 必須カラム確認
- `record_date` の重複確認
- 数値として不自然な値の確認
- 空欄が多い記録の確認

不自然な値を自動削除しないでください。確認表示に留めてください。

## Config Plan

将来的に `config.json` に対応してください。

設定例:

```json
{
  "default_jardiance": "有",
  "default_water_amount": "普通",
  "high_sys_threshold": 140,
  "high_dia_threshold": 90,
  "low_sys_threshold": 100,
  "auto_backup": true
}
```

設定ファイルが存在しない場合は、標準設定で起動してください。設定ファイル破損時は、日本語エラーメッセージを表示してください。

## Report Plan

診察用レポートはまず HTML 出力を優先してください。PDF 出力は後回しで構いません。

レポート内容候補:

- 対象期間
- 記録日数
- 朝の平均血圧
- 最高血圧記録
- 最低血圧記録
- 立ちくらみがあった日
- 背部痛があった日
- Hi-Level Sample一覧
- Low-Level Sample一覧
- メモ欄の抜粋
- 医師に相談したいポイント

夜の平均血圧は、夜の血圧記録項目を追加するまで未対応表示で構いません。

## Input Aid Plan

毎日の入力を楽にするため、以下を段階的に追加してください。

- 今日の日付を自動入力
- 記録日カレンダー選択
- 前回入力値の一部コピー
- コンサータ服用時刻の候補ボタン
- メトグルコ服用回数の候補ボタン
- よく使うメモのテンプレート
- 入力漏れ確認
- 保存前の確認表示

## Do Not

- 医療診断を断定すること
- 「高血圧です」「起立性低血圧です」と確定表示すること
- ネットワーク通信を必須にすること
- Google Sheets API を最初から必須にすること
- 体重入力を必須にすること
- 保存済みデータを確認なしで削除すること
- 韓国語表示を入れること
- データを勝手に削除すること

## Additional Development Notes

優先順位:

1. データ保存の安定性
2. 入力のしやすさ
3. 表示の見やすさ
4. CSV出力
5. 期間指定
6. グラフ
7. 抽出・分析
8. レポート
9. バックアップ
10. exe化

方針:

- まずは毎日続けられることを優先する
- 入力項目は増やしすぎない
- 任意入力を基本にする
- 医療診断ではなく、医師に相談するための記録補助に徹する
- エラー時は日本語で原因を表示する
- データを勝手に削除しない
- 1つの機能追加ごとに動作確認する

## Suggested Next Task for Codex

現在の実装状況と `SCRIPT_STEPS.md` を確認し、未実装の機能を小さく追加してください。

推奨順序:

1. 入力補助
2. 手動バックアップ
3. データ修復・整合性チェック
4. 設定ファイル
5. レポート強化
6. グラフ拡張
7. Excel出力
8. exe化

---

## 追記:

### 姿勢変化イベント記録

しゃがみ込み、立ち上がり、入浴後、運動後など、特定動作の前後で血圧が大きく変動する可能性があるため、通常の朝測定とは別に「姿勢変化イベント記録」を追加する。

記録項目：

- event_date
- event_time
- event_type
  - 冷凍庫前しゃがみ込み
  - 入浴後
  - 自転車後
  - 階段後
  - 食後
  - その他
- before_sys
- before_dia
- before_pulse
- after_sys
- after_dia
- after_pulse
- after_1min_sys
- after_1min_dia
- after_1min_pulse
- symptom
  - なし
  - 軽い立ちくらみ
  - 強い立ちくらみ
  - 背部痛
  - 息切れ
  - その他
- memo

自動計算：

- 収縮期差分 = before_sys - after_sys
- 拡張期差分 = before_dia - after_dia

表示：

- 収縮期差分が20mmHg以上なら「確認推奨」
- 収縮期血圧が90mmHg未満なら「低値注意候補」
- 症状がある場合は「症状あり」と表示

注意：

この機能は医療診断ではなく、医師に相談するための記録補助とする。